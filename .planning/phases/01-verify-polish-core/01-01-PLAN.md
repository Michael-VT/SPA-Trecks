---
phase: 01-verify-polish-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/
  - README.md
  - style.css
autonomous: true
requirements:
  - MOB-04
must_haves:
  truths:
    - "Empty libs/ directory and empty README.md are deleted from the repo"
    - "No console errors on page load with python3 -m http.server"
    - "All ES module imports resolve without 404s"
  artifacts:
    - path: "style.css"
      provides: "Responsive layout with @media(max-width:768px) rules"
      contains: "@media"
  key_links:
    - from: "index.html"
      to: "app.js"
      via: 'script type="module"'
      pattern: 'type="module"'

---

<objective>
Wave 1: Cleanup dead files and verify static correctness — syntax, imports, CDN loading, responsive CSS.

Purpose: Remove empty artifacts that add confusion. Confirm the app boots without errors before deeper functional verification. Per D-11 delete empty libs/, per D-12 delete empty README.md.

Output: Clean repo with zero console errors on startup. All 3 CDN libs (Leaflet, Chart.js, Hammer.js) load. All ES module imports resolve. Responsive CSS rules present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verify-polish-core/01-CONTEXT.md

<interfaces>
From index.html — CDN globals loaded as <script> tags (not modules):
  - L (Leaflet 1.9.4 from unpkg)
  - Chart (Chart.js from jsdelivr)
  - Hammer (Hammer.js from jsdelivr)

From app.js — ES module imports from local modules:
  - loadTracks from ./modules/parser.js
  - applyDriftCorrection from ./modules/drift.js
  - drawTracks, movePlaybackMarker, setupTooltip from ./modules/renderer.js
  - createCharts, updateChartCursor, bindChartToMap from ./modules/charts.js
  - calculateStatistics, showStatisticsTable, hideStatisticsTable from ./modules/statistics.js
  - * as pb from ./modules/playback.js
  - exportCSV, exportJSON, exportGPX from ./modules/export.js
  - enableMobileGestures from ./modules/mobile.js
  - initUI, toggleFullscreen from ./modules/ui.js
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete empty artifacts and verify static loading</name>
  <files>libs/, README.md</files>
  <action>
Per D-11 and D-12:

1. Delete the empty `libs/` directory (contains 0-byte `hammer.min.js` and `chart.min.js`; CDN is the actual source).
2. Delete the 0-byte `README.md` file.

Then verify static correctness:
3. Run `node --check` on every .js file (app.js + all 9 modules) to confirm no syntax errors.
4. Grep all `import` statements in app.js and modules/*.js — confirm every import path exists on disk.
5. Grep index.html for the 3 CDN script src URLs — confirm they reference valid unpkg/jsdelivr URLs for Leaflet 1.9.4, Chart.js, and Hammer.js.
6. Confirm style.css contains `@media(max-width:768px)` rules for responsive layout (MOB-04).
7. Serve with `python3 -m http.server 8080` and open in browser — verify zero console errors on page load, map renders, track loads, route is drawn.
  </action>
  <verify>
    <automated>node --check app.js && node --check modules/parser.js && node --check modules/renderer.js && node --check modules/statistics.js && node --check modules/charts.js && node --check modules/playback.js && node --check modules/drift.js && node --check modules/export.js && node --check modules/mobile.js && node --check modules/ui.js && echo "ALL SYNTAX OK"</automated>
  </verify>
  <done>
    - libs/ directory deleted (no 0-byte stubs remain)
    - README.md deleted (was 0 bytes)
    - All 10 JS files pass node --check
    - All ES module imports resolve to existing files
    - style.css has @media responsive rules
    - Page loads with python3 -m http.server, zero console errors, route visible on map
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | External scripts from unpkg/jsdelivr — trust CDN integrity |
| file:// / http.server → browser | Static serving of local modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | CDN scripts | accept | No SRI hashes in current HTML; no sensitive data in app |
| T-01-02 | T | http.server | accept | Local dev only, no production deployment |

Notes: This is a verification phase, not new feature work. No new trust boundaries introduced.
</threat_model>

<verification>
1. `test ! -d libs/` — libs/ deleted
2. `test ! -f README.md` — empty README deleted
3. All JS files pass `node --check`
4. Browser console: 0 errors on load
5. Map renders with route visible
</verification>

<success_criteria>
- Empty libs/ and README.md removed from repo
- All source files pass syntax check
- App boots without errors from `python3 -m http.server`
</success_criteria>

<output>
After completion, create `.planning/phases/01-verify-polish-core/01-01-SUMMARY.md`
</output>
