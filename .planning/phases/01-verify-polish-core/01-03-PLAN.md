---
phase: 01-verify-polish-core
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - modules/charts.js
  - modules/playback.js
  - modules/renderer.js
  - modules/export.js
  - modules/ui.js
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04
  - CHART-01
  - CHART-02
  - CHART-03
  - CHART-04
  - CHART-05
  - HUD-01
  - HUD-02
  - TIP-01
  - TIP-02
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03
must_haves:
  truths:
    - "Chart panel shows speed, elevation, HR lines with dual Y-axis"
    - "Clicking on chart moves playback marker on map"
    - "Hovering on chart updates HUD"
    - "Play button starts marker animation along route"
    - "Pause button stops animation"
    - "Spacebar toggles play/pause"
    - "Arrow keys seek forward/backward by 20 points"
    - "HUD updates speed, HR, elevation, time during playback and scrubbing"
    - "Tooltip appears after 2-second hover over route point"
    - "CSV export downloads file with all point data"
    - "JSON export downloads complete track array"
    - "GPX export downloads valid GPX XML"
---

<objective>
Wave 3: Verify interactive features — playback, charts, HUD, tooltip, export.

Purpose: Confirm all user-interactive features work correctly: the playback engine animates the marker, chart scrubber syncs with the map, HUD shows real-time data, tooltip shows on hover, and all export formats download correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verify-polish-core/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify playback engine and HUD</name>
  <files>modules/playback.js, modules/renderer.js, modules/ui.js</files>
  <action>
1. With app loaded, click ▶ button.
2. Verify yellow marker starts moving along the route.
3. Verify HUD updates: speed changes, elevation changes, time updates.
4. Click ⏸ button — verify marker stops.
5. Press Spacebar — verify playback toggles (starts if stopped, stops if running).
6. Press ArrowRight — verify marker jumps forward ~20 points.
7. Press ArrowLeft — verify marker jumps backward.
8. Verify marker stays within route bounds (doesn't go past end or before start).
9. Let playback run to end — verify it stops when reaching last point.
  </action>
  <verify>
    - Marker moves along route during playback
    - HUD updates with speed/elevation/time
    - Spacebar toggles play/pause
    - Arrow keys seek correctly
    - Playback stops at end
  </verify>
  <done>
    - Playback engine works correctly
    - HUD reflects current position data
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify chart panel and sync</name>
  <files>modules/charts.js</files>
  <action>
1. Press G key or click 📈 button.
2. Verify chart panel slides up from bottom.
3. Verify chart shows 3 datasets: Speed (blue), Elevation (green), HR (red).
4. Verify dual Y-axis: speed/HR on left, elevation on right.
5. Hover mouse over chart — verify:
   - Playback marker on map moves to corresponding position
   - HUD updates with that point's data
6. Click on chart — verify marker snaps to clicked position.
7. Press G again — verify chart panel slides down.
  </action>
  <verify>
    - Chart shows speed/elevation/HR lines
    - Hovering chart moves map marker
    - Clicking chart snaps marker
    - Chart toggles with G key
  </verify>
  <done>
    - Chart renders all datasets
    - Chart↔map sync works bidirectionally
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify tooltip</name>
  <files>modules/renderer.js</files>
  <action>
1. With chart panel hidden, move mouse over the route on the map.
2. Hold mouse still over a route point for 2+ seconds.
3. Verify tooltip appears showing:
   - Point index number
   - Speed in km/h
   - Elevation in meters (corrected)
   - HR (likely "—" since no HR data)
   - Time
   - Distance in km
   - Coordinates (lat, lon)
4. Move mouse away from route — verify tooltip disappears.
5. Move mouse over route but quickly away (<2 sec) — verify tooltip does NOT appear.
  </action>
  <verify>
    - Tooltip appears after 2s hover
    - Shows all point data fields
    - Disappears when mouse leaves route
  </verify>
  <done>
    - Tooltip with 2-second delay works correctly
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify export functions</name>
  <files>modules/export.js</files>
  <action>
1. Press X key — verify CSV file downloads as `track.csv`.
2. Open the CSV file and verify:
   - Header row: index,time,lat,lon,speed,hr,ele,distance
   - Data rows have correct number of columns
   - First row index = 0, last row ≈ 6681
   - Distance values are cumulative
3. Press Z key — verify JSON file downloads as `track.json`.
4. Verify JSON is valid array of objects with expected properties.
5. Press E key — verify GPX file downloads as `track.gpx`.
6. Verify GPX XML structure: <trkpt lat=... lon=...> with <ele> and <time>.
7. Verify exported elevation uses corrected values (eleCorrected, not raw ele).
  </action>
  <verify>
    - CSV downloads with correct headers and data
    - JSON downloads as valid array
    - GPX downloads as valid XML with corrected elevation
  </verify>
  <done>
    - All 3 export formats work correctly
  </done>
</task>

</tasks>

<verification>
1. Playback marker moves smoothly and syncs with chart/HUD
2. Chart panel shows all datasets with cursor sync
3. Tooltip appears after 2s with point data
4. All 3 export formats download valid data
</verification>

<success_criteria>
- Playback, chart sync, HUD all work together
- Tooltip shows on 2s hover
- CSV/JSON/GPX exports contain correct data
</success_criteria>

<output>
After completion, create `.planning/phases/01-verify-polish-core/01-03-SUMMARY.md`
</output>
