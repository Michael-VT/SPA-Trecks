---
phase: 01-verify-polish-core
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - modules/mobile.js
  - modules/renderer.js
  - modules/parser.js
autonomous: true
requirements:
  - MOB-01
  - MOB-02
  - MOB-03
  - MOB-04
must_haves:
  truths:
    - "Swipe left/right switches color modes on mobile"
    - "Swipe up/down toggles chart panel"
    - "Double tap toggles play/pause"
    - "Responsive layout adapts to mobile screens"
    - "App loads and renders on Android Termux browser"
    - "Broken XML files show warning, don't crash app"
    - "Missing track files show warning, don't crash app"
---

<objective>
Wave 4: Cross-platform verification + edge cases.

Purpose: Verify mobile gestures work, responsive layout is correct, and the app handles edge cases gracefully (broken files, missing files, no CDN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verify-polish-core/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify mobile gestures and responsive layout</name>
  <files>modules/mobile.js, style.css</files>
  <action>
1. Open app in Chrome DevTools mobile emulator (or real Android device).
2. Swipe left — verify mode switches to speed (if not already).
3. Swipe right — verify mode switches to height.
4. Swipe up — verify chart panel appears.
5. Swipe down — verify chart panel hides.
6. Double-tap — verify playback toggles.
7. Verify responsive layout:
   - HUD text is smaller (11px)
   - Chart panel shorter (180px)
   - Playback controls adjust position
   - HUD keyboard hint changes to "Свайп: режимы | 2×тап: воспр."
8. Long-press on map — verify console logs "LONG PRESS" (no crash).
  </action>
  <verify>
    - All swipe gestures trigger correct actions
    - Double-tap toggles playback
    - Responsive CSS adapts layout for mobile
  </verify>
  <done>
    - Mobile gestures functional
    - Responsive layout correct
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify edge cases — broken and missing files</name>
  <files>modules/parser.js, app.js</files>
  <action>
1. Create a broken XML file: `echo "NOT XML" > tracks/broken.gpx`
2. Temporarily add 'tracks/broken.gpx' to the loadTracks array in app.js.
3. Reload app — verify:
   - Console shows warning "Failed to load tracks/broken.gpx"
   - App does NOT crash
   - Other tracks still load and render
4. Revert app.js (remove broken.gpx from array).
5. Test with empty file: `touch tracks/empty.tcx`
6. Add 'tracks/empty.tcx' to loadTracks, reload — verify no crash, warning shown.
7. Revert app.js.
8. Test offline CDN: block CDN domains in DevTools Network tab.
9. Reload — verify app shows error gracefully (Leaflet/Chart.js don't load, but no white-screen crash).
10. Clean up: delete broken.gpx and empty.tcx.
  </action>
  <verify>
    - Broken XML → warning, no crash, other tracks load
    - Empty file → warning, no crash
    - Missing CDN → graceful degradation
  </verify>
  <done>
    - Edge cases handled gracefully
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify on Android Termux</name>
  <files></files>
  <action>
1. SSH into Android Termux device.
2. Clone/copy project to Termux filesystem.
3. Run `python3 -m http.server 8080`.
4. Open browser on phone to `http://localhost:8080`.
5. Verify:
   - Map renders with track
   - Color gradient visible
   - Touch gestures work (swipe, double-tap)
   - Chart panel opens/closes
   - No console errors
6. If issues found, document and fix.
  </action>
  <verify>
    - App loads on Android browser
    - Touch interactions work
    - Route renders correctly
  </verify>
  <done>
    - App works on Android Termux
  </done>
</task>

</tasks>

<verification>
1. Mobile gestures work in emulator/device
2. Broken files handled gracefully
3. App runs on Android Termux
</verification>

<success_criteria>
- Mobile gestures and responsive layout verified
- Edge cases handled without crashes
- App functional on Android Termux
</success_criteria>

<output>
After completion, create `.planning/phases/01-verify-polish-core/01-04-SUMMARY.md`
</output>
