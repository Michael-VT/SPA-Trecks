---
phase: 01-verify-polish-core
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - modules/parser.js
  - modules/renderer.js
  - modules/statistics.js
  - modules/drift.js
autonomous: true
requirements:
  - LOAD-01
  - LOAD-02
  - LOAD-03
  - LOAD-04
  - LOAD-05
  - LOAD-06
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05
  - VIS-06
  - DRIFT-01
  - DRIFT-02
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
must_haves:
  truths:
    - "GPX file loads 6682 points with lat/lon/ele/time"
    - "TCX file loads 6682 points with same data"
    - "KML file loads 6682 data points (skips 2 Start/End markers)"
    - "Primary track = longest of 3 files, NOT concatenated (~9.66km total distance)"
    - "Speed computed from haversine + time deltas (km/h)"
    - "Cumulative distance computed along route"
    - "Route renders with color gradient: green(slow)→yellow→red(fast) for speed mode"
    - "Route renders with color gradient for height mode"
    - "Legend shows gradient bar with min/max values and units"
    - "Drift correction smooths altitude (±5 window) and corrects start=end for loops"
    - "Statistics table shows ~9.66km distance (NOT ~29km)"
    - "Statistics shows elevation gain/loss, avg/max speed, min/max elevation"
---

<objective>
Wave 2: Verify core data pipeline — loading, parsing, rendering, statistics, drift correction.

Purpose: Confirm the entire data pipeline from file loading through color-coded rendering and statistics works correctly. This is the heart of the application.

Output: All 3 file formats load correctly. Color gradient is visually correct. Statistics are accurate. Drift correction produces start=end elevation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verify-polish-core/01-CONTEXT.md

<interfaces>
Track data schema (from parser.js):
  { lat: Number, lon: Number, ele: Number|null, time: String|null, hr: Number|null, speed: Number, distance: Number }
After drift correction adds: eleSmooth, eleCorrected
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify track loading and data pipeline</name>
  <files>modules/parser.js, tracks/</files>
  <action>
1. Start http server (`python3 -m http.server 8080`).
2. Open browser to `http://localhost:8080`.
3. Open browser DevTools Console.
4. Verify no errors during track loading.
5. In console, check that tracks array has 3 entries:
   - `tracks[0].length` should be ~6682 (GPX)
   - `tracks[1].length` should be ~6682 (TCX)
   - `tracks[2].length` should be ~6682 (KML)
6. Verify primary track picks the longest: `primary.length` should equal max of the three.
7. Verify `primary[0].speed === 0` (first point has no speed).
8. Verify `primary[primary.length-1].distance` ≈ 9660 (meters, ±5%).
9. Verify all points have `lat`, `lon`, `distance`, `speed` properties.
10. Verify `eleCorrected` exists on all points (drift correction applied).
11. Verify `primary[0].eleCorrected` ≈ `primary[primary.length-1].eleCorrected` (drift corrected for loop).
  </action>
  <verify>
    - Console shows no errors
    - 3 tracks loaded, each ~6682 points
    - Total distance ~9.66km (not ~29km)
    - eleCorrected[0] ≈ eleCorrected[last]
  </verify>
  <done>
    - All 3 file formats parse correctly
    - Speed and distance computed
    - Drift correction applied
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify color-coded rendering and legend</name>
  <files>modules/renderer.js, style.css</files>
  <action>
1. With the app loaded, press S key for speed mode.
2. Verify route is drawn with color gradient: green sections for slow, red for fast.
3. Verify legend in bottom-right shows:
   - Title: "Скорость"
   - Gradient bar from green to red
   - Min speed value in km/h
   - Max speed value in km/h
4. Press H for height mode.
5. Verify route colors change to elevation gradient.
6. Verify legend updates to "Высота" with meters.
7. Press P for pulse mode.
8. Verify route renders (likely uniform color since no HR data).
9. Verify legend shows "Пульс" with bpm units.
10. Verify map auto-fits to track bounds on load.
  </action>
  <verify>
    - Speed mode: green→red gradient visible
    - Height mode: different gradient visible
    - Legend updates per mode with correct units
    - Map fits track bounds
  </verify>
  <done>
    - Color gradients render correctly for all 3 modes
    - Legend displays accurate min/max with units
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify statistics and drift correction</name>
  <files>modules/statistics.js, modules/drift.js</files>
  <action>
1. Press T key to open statistics overlay.
2. Verify statistics table shows:
   - Расстояние: ~9.66 km (NOT ~29 km)
   - Длительность: ~1:51:59 (6719 seconds)
   - Ср. скорость: reasonable value (~5-6 km/h for running)
   - Макс. скорость: reasonable peak
   - Набор высоты: positive value
   - Потеря высоты: similar positive value (loop route)
   - Мин/Макс высота: within expected range
   - Точек: 6682
   - Пульс: should show "—" (no HR data)
3. Verify drift correction:
   - In console, check `primary[0].eleCorrected` and `primary[primary.length-1].eleCorrected`
   - They should be approximately equal (loop route returned to start)
   - Raw values differ: `primary[0].ele` = 49.1, `primary[primary.length-1].ele` = 35.4
4. Press T again or click backdrop to close statistics.
  </action>
  <verify>
    - Statistics shows correct distance ~9.66km
    - Duration ~1h52m
    - Elevation gain ≈ elevation loss (loop)
    - Drift-corrected start ≈ drift-corrected end
  </verify>
  <done>
    - Statistics accurate for single-route (not 3x)
    - Drift correction produces correct loop profile
  </done>
</task>

</tasks>

<verification>
1. Console log: track point counts match expectations
2. Visual: color gradient visible on route
3. Statistics: distance ~9.66km, not ~29km
4. Drift: eleCorrected[0] ≈ eleCorrected[last]
</verification>

<success_criteria>
- All 3 track formats load with correct point counts
- Color gradient renders for speed/height/pulse modes
- Statistics show correct single-route distance
- Drift correction equalizes start/end elevation
</success_criteria>

<output>
After completion, create `.planning/phases/01-verify-polish-core/01-02-SUMMARY.md`
</output>
